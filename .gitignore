# OS-мусор
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
.DS_Store

# Редакторы
.vscode/
.idea/
*.swp

# Логи / временное
*.log
node_modules/

# Секреты и сборка
.env
.env.local
dist/

# Внутренние документы — НЕ публикуем (остаются локально на твоём компьютере)
SPEC.md
PLAN.md
TASK.md
BOT_SPEC.md

# Бот — отдельный компонент, получит свой репозиторий (в репо сайта не публикуем)
bot/

# Папку .claude/ НЕ игнорируем — агенты, скиллы и CLAUDE.md едут вместе с проектом
